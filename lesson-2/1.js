const a = 2;
const b = 1;

let result = (a + b < 4) ? true : false;

console.log(result);
/*
такая запист может быть упрощенна до `let result = (a + b < 4)`
т.к. результатом выражения будет либо `true` либо `false`
*/